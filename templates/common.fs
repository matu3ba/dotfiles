// file endings: .fs, .fsi, .fsx, .fsscript
// https://danielbachler.de/2020/12/23/what-i-wish-i-knew-when-learning-fsharp.html
// F# is better Python, but biggest disadvantage is no big application/community like AI/ML behind it and thus worse tooling + no package manager.
// C# compiling to .NET runtime looks also faster than Java with F# offering several optimization options.
// In simple benchmarks C++ was 3x faster.
// OCaml has comparable performance with what looks like a more dense syntax.
